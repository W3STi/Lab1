#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<conio.h>
int main(){
	printf("Enter array: \n");
	int n, i = 0;
	scanf("%d", &n);
	int *a = (int*)malloc(n * sizeof(int));
	while(i != n){
	scanf("%d", &a[i]);
	printf("Digit - %d\n", a[i]);
	i++;
	}
	free(a);
	_getch();
}
