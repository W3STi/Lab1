#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<stdlib.h>
#include<conio.h>

int main(){
	srand(time(NULL));
	int arr[10];
	for(int j = 0; j < 10; j++){
		arr[j] = rand()%10;
		printf("%d\n",arr[j]);
}
int min = arr[0], max = arr[0], tmp;
for(int j = 1; j < 10; j++){
	if (arr[j] > max){
		max = arr[j];
	}
}
for(int j = 1; j < 10; j++){
	if (arr[j] < min){
		min = arr[j];
	}
}
printf(" %d", max - min);
_getch();
}
